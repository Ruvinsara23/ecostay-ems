'use client';

import { AdminAlerts } from '@/admin/admin-alerts';

/** /admin/alerts — fleet-wide alert centers, acknowledge in one place. */
export default function AdminAlertsPage() {
  return <AdminAlerts />;
}
