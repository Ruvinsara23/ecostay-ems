'use client';

import { AdminDevices } from '@/admin/admin-devices';

/** /admin/devices — read-only fleet device registry; credentials live in property detail. */
export default function AdminDevicesPage() {
  return <AdminDevices />;
}
