'use client';

import { AdminProperties } from '@/admin/admin-properties';

/** /admin/properties — the property registry: browse, drill in, register. */
export default function AdminPropertiesPage() {
  return <AdminProperties />;
}
